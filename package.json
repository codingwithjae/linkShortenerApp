{
  "name": "linkshortenerapp",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:styles": "postcss src/styles.css -o public/build/styles.css",
    "watch:styles": "postcss src/styles.css -o public/build/styles.css --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  },
  "dependencies": {
    "firebase": "^10.5.2",
    "flowbite": "^2.3.0"
  },
  "description": "This a link shortening app using the Clean URI API. It was made with HTML, Tailwind CSS and Vanilla JS"
}
